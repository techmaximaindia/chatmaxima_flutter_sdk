name: chatmaxima_sdk_example
description: Runnable demo of the ChatMaxima Flutter SDK.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.3 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

  chatmaxima_flutter_sdk:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
